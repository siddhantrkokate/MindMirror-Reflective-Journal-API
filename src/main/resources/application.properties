# ==== Spring datasource (MySQL) ====

# Uncomment these lines if you want to test without MySQL (H2 in-memory DB)
# spring.datasource.url=jdbc:h2:mem:mindmirror;DB_CLOSE_DELAY=-1
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# Use Railway's dynamic port
server.port=${PORT:8080}

# MySQL connection (with fix for Public Key Retrieval error)
spring.datasource.url=jdbc:mysql://centerbeam.proxy.rlwy.net:49053/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=NmWrOpTjKMoZTzJFdiELfYOWQAKYamcC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate auto-update schema
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Connection pool (HikariCP default settings)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# Gemini API Configuration
gemini.api.key=AIzaSyC7uOUk603kSUUKsomgbvROvmuP0jEJQ68
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent

# Optional: Logging
logging.level.org.springframework=INFO
logging.level.com.mindmirror=DEBUG